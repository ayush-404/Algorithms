#include <iostream>
#include <vector>
using namespace std;
typedef long long ll;

int main() {
  int n;
  cin >> n;
  ll p[20], ans,s=0;
  for(int i = 0; i < n; i++) {
    cin >> p[i];
    s += p[i];
  }
  
}
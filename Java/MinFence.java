class MinFence {
  public int[][] outerTrees(int[][] trees) {
      int[] current = trees[0];
      int[][] ret = new int[3000][2];
      boolean boundLeft = false;
      boolean boundRight = false;
      ret[0] = current;
      for(int i =1 ; i < trees.length; i++) {
        boundLeft = false ; boundLeft = false;
        for(int j =0; j < ret.length; j++) {
          if(ret[j][0] < )
        }
      }
  }
}